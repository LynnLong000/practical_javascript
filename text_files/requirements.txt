// c means checked 

V1 Requirements
- It should have a place to store todos      c 
- It should have a way to display todos      c 
- It should have a way to add new todos      c 
- It should have a way to change a todos     c
- It should have a way to delete a todos     c

V2
- It should have a function to display todos  c
- It should have a function to add todos      c
- It should have a function to change todos   c 
- It should have a function to delete todos   c 

v3 
- It should store the todos array on an object    c
- It should have a displayTodos method            c
- It should have an addTodos  method              c 
- It should have a changeTodos method             c
- It should have a deleteTodos method             c

v4
- todoList.addTodo should add objects                             C
- todoList.changeTodo should change the todoText property         C
- todoList.toggleCompleted should change the completed property   C

v5 
- .displayTodos should show .todoText                     c
- .displayTodos should tell you if .todos is empty        c
- .displayTodos should show .completed                    c    

v6
- .toggleAll: if everything's true, make everything false  c
- .toggleAll: otherwis, make everything true               c


v7
- there should be a "display todos " button and a "toggle all" button      c 
- Clicking "display todos" should run todolist.displayTodos.               c  
- Clicking "toggle all" should run todoList.toggleAll.                     c


v8
- It should have working controls for .addTodo             C
- It should have working controls for .changeTodo          C
- It should have working controls for .deleteTodo          C
- It should have working controls for .toggleCompleted     C

V9
- There should be an li element for every todoList   c
- Each li element should contain .todoText           c
- Each li element should show .completed             c

v10
- there should be a way to create delete buttons           c
- there should be a delete button for each todoList        c
- each li should have an id that has the todo position     c
- delete buttons should have access to the todo id         c
- Clicking delete should update todoList.tdos and the DOM  c

v11
- todoList.toggleAll should use for each   c
- view.displayTodos should use for each    c